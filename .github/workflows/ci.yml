name: SoftFocus CI

on: [push, pull_request]

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Backend CI gate + pipeline check
        run: |
          cd backend
          alias_softfocus/bin/ci-gate
          alias-softfocus run pipeline-check

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Frontend test + build
      - name: Generate alias status
  run: npm run alias:status

- name: Alias gate
  run: alias-softfocus run project-clean

        run: |
          cd frontend
          npm ci
          npm test
          npm run build
