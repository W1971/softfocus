import { buildMetadata } from "@/frontend/seo/buildMetadata";

export const metadata = buildMetadata({
  title: "Sleep Calculator — SoftFocus",
  description:
    "Non-medical assessment of sleep-related recovery and burnout risk under sustained cognitive load.",
  locale: "en-US",
  indexable: true
});
