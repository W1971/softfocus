aliases:
  bundle:
    description: Generate and validate canonical project bundle
    status: READY
    execution: ALLOWED
    affects_readiness: true
    scripts:
      - scripts/bundles/generate_project_bundle.sh
    ci_gates:
      - scripts/ci/guards/bundle-freshness-guard.sh
      - scripts/ci/guards/bundle-diff-guard.sh

  stage-1:
    description: Mobile UI visual gate (design-level)
    status: DESIGN_ONLY
    execution: FORBIDDEN
    affects_readiness: true

  stage-2:
    description: Mobile UI semantic gate (design-level)
    status: DESIGN_ONLY
    execution: FORBIDDEN
    affects_readiness: true

  stage-3:
    description: Mobile UI interaction gate (design-level)
    status: DESIGN_ONLY
    execution: FORBIDDEN
    affects_readiness: true
